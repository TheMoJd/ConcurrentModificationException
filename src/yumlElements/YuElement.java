package yumlElements;

/**
 * Classe mère éventuelle de tous les éléments "YuElement"
 * (Hypothèse : le diagramme mentionne "Yu Element Package")
 */
public abstract class YuElement {
    // On peut imaginer qu’il y ait ici un "id", un "label", etc.
    // L’UML ne donnait pas de détail, on laisse vide ou minimaliste.
}
