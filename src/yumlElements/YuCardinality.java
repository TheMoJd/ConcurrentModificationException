package yumlElements;

/**
 * Classe abstraite représentant la cardinalité
 * (ex: 0..1, 1..*, etc.)
 */
public abstract class YuCardinality extends YuElement {
    // On ne sait pas s’il y a un attribut commun.
    // Le diagramme laisse entendre que c’est une super-classe.
}
